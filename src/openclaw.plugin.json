{
  "id": "policy-layer",
  "name": "Policy Layer (CBS)",
  "description": "Springdrift-inspired CBS: injects <openclaw_state> before each LLM call. D' = Σ(w·mag)/(0.3×1.0×n). Signals: success_rate (w=0.30), tool_success (w=0.25), cbr_hit (w=0.20), severity_inversion (w=0.25). D' bands: <0.35 LOW_ACCEPT, 0.35–0.55 MEDIUM_CONFIRM, ≥0.55 HIGH_REJECT.",
  "version": "0.4.0",
  "kind": "sensorium",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "reportToUser": {
        "type": "boolean",
        "default": true,
        "description": "Agent reports CBS status (D', gate, last_result) in conversation after tool calls"
      },
      "sensoriumWindow": {
        "type": "number",
        "minimum": 1,
        "default": 20,
        "description": "Number of recent cycles to consider for metrics"
      },
      "dGateThreshold": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.35,
        "description": "D' threshold below which actions are gated"
      },
      "logLevel": {
        "type": "string",
        "enum": ["debug", "info", "warn"],
        "default": "info"
      }
    }
  },
  "uiHints": {
    "sensoriumWindow": {
      "label": "CBS Sensorium Window",
      "help": "Number of recent cognitive cycles to consider for CBS signal computation"
    },
    "dGateThreshold": {
      "label": "D' Gating Threshold",
      "help": "D' score below which the agent should slow down and reassess strategy"
    },
    "logLevel": {
      "label": "Log Level",
      "help": "Verbosity of policy-sensorium logging"
    }
  }
}
