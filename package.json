{
  "name": "policy-layer",
  "version": "0.2.0",
  "type": "module",
  "description": "OpenClaw policy enforcement plugin: Layer 1 (normalize + 23 dangerous patterns), Layer 2 (D' CBS sensorium), Layer 3 (LLM smart-review + fast-lane + JSONL log), Layer 4 (39 secret patterns redaction)",
  "main": "src/index.ts",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/ws": "^8.5.10",
    "vitest": "^3.2.4",
    "ws": "^8.20.0"
  }
}
